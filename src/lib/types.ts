export type CacheSource = "fresh-cache" | "stale-cache" | "api" | "mock";
export type RefreshBlockedReason = "rate-limit" | "daily-budget" | "lock-active" | "api-error";
export type CacheMeta = { source: CacheSource; fetchedAt: string; expiresAt?: string; isStale: boolean; refreshBlockedReason?: RefreshBlockedReason };
export type Cached<T> = { value: T; fetchedAt: string; freshUntil: string; staleUntil: string };
export type PlayerProfile = { playerId: string; nickname: string; level?: number; rank?: string; avatarUrl?: string };
export type PlayerSummary = { playerId: string; winRate: number; matchesPlayed: number; favoriteHero?: string; kda?: number };
export type PlayerHeroStat = { heroId: string; heroName: string; matches: number; winRate: number; pickRate?: number; kda?: number };
export type PlayerMatch = { matchId: string; playedAt: string; result: "WIN" | "LOSS"; heroName: string; kda?: number; map?: string };
export type PlayerDetail = { profile: PlayerProfile; summary?: PlayerSummary; heroes?: PlayerHeroStat[]; matches?: PlayerMatch[] };
export type WithMeta<T> = { data: T; cache: CacheMeta };
